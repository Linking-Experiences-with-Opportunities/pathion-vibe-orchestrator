# =========================
# questions-api — Local Dev
# =========================

# Port the backend listens on locally
PORT="1323"

# MongoDB connection string (SECRET) — do NOT commit real value
MONGO_URI="mongodb://localhost:27017"

# Mongo DB names (not secrets)
MONGO_DB_CONTENT="content_db_dev"
MONGO_DB_APP="app_db_dev"
MONGO_DB_APP_DEV="app_db_dev"
MONGO_DB_APP_STAGING="lilolp_staging"

# CORS allowed origins (comma-separated)
ALLOWED_ORIGINS="http://localhost:3000"

# Supabase (secrets unless explicitly anon/public)
SUPABASE_URL="https://YOUR_PROJECT.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="SUPABASE_SERVICE_ROLE_KEY_PLACEHOLDER"
SUPABASE_JWT_SECRET="SUPABASE_JWT_SECRET_PLACEHOLDER"

# Webhook secret (SECRET)
WHITELIST_WEBHOOK_SECRET="WHITELIST_WEBHOOK_SECRET_PLACEHOLDER"

# If/when confirmed by code usage:
# SUPABASE_WEBHOOK_SECRET="SUPABASE_WEBHOOK_SECRET_PLACEHOLDER"
NODE_ENV="staging"