# =========================
# mvp-web-app — Local Development (.env.example)
# =========================
# Next.js loads env vars from .env* files. Variables prefixed with NEXT_PUBLIC_
# are exposed to the browser (so never put secrets in them). :contentReference[oaicite:0]{index=0}

# (NEXT_PUBLIC) Backend API base URL used by the frontend.
# Local dev typically points to a local backend.
NEXT_PUBLIC_BACKEND_API_URL="http://localhost:1323"

# (NEXT_PUBLIC) Cloudflare Python Worker runner base URL.
# Example (staging): https://cf-runner-staging.linkedinorleftout.workers.dev
# NEXT_PUBLIC_CF_RUNNER_URL=""

# (NEXT_PUBLIC) Supabase project URL used by browser code to initialize the client.
NEXT_PUBLIC_SUPABASE_URL="https://YOUR_PROJECT.supabase.co"

# (NEXT_PUBLIC) Supabase anon/public key (browser-exposed; not a service-role secret).
NEXT_PUBLIC_SUPABASE_ANON_KEY="SUPABASE_ANON_KEY_PLACEHOLDER"

# (NEXT_PUBLIC) Pyodide asset base URL (where the browser downloads Pyodide files from).
#NEXT_PUBLIC_PYODIDE_URL: This is a conditional env var, not required for every use case.

# (NEXT_PUBLIC) Feature flag: force browser execution (even for hidden tests).
#NEXT_PUBLIC_FORCE_LOCAL: This is a conditional env var, not required for every use case.

# (NEXT_PUBLIC) Feature flag: force server execution (overrides FORCE_LOCAL).
#NEXT_PUBLIC_FORCE_SERVER: This is a conditional env var, not required for every use case.

# (NEXT_PUBLIC) Feature flag: show/hide the runner panel UI.
#NEXT_PUBLIC_SHOW_RUNNER_PANEL: This is a conditional env var, not required for every use case.

# (NEXT_PUBLIC) Feature flag: enable debug runner page.
# NEXT_PUBLIC_ENABLE_DEBUG_RUNNER="false"

# (NEXT_PUBLIC) Feature flag: enable visual debugger (Mermaid diagrams & AI trace).
# NEXT_PUBLIC_FEATURE_MERMAID_DEBUGGER="true"

# (NEXT_PUBLIC) Feature flag: enable trace timeline (scrubber for execution history).
# NEXT_PUBLIC_FEATURE_TRACE_TIMELINE="true"

# (NEXT_PUBLIC) Feature flag: enable step-through debugger (debugger worker).w
# NEXT_PUBLIC_FEATURE_STEP_DEBUGGER="true"

# (NEXT_PUBLIC) Feature flag: enable challenge mode (cognitive mirror intervention).
# NEXT_PUBLIC_FEATURE_CHALLENGE_MODE="true"

# (NEXT_PUBLIC) CloudFront distribution URL for S3 media assets (videos, etc).
# Defaults to production CloudFront if not set.
#NEXT_PUBLIC_CLOUDFRONT_BASE_URL="https://d158alpjmt7vrd.cloudfront.net"

# Airtable base (identifier/config).
#AIRTABLE_BASE_ID:  This is a conditional env var, not required for every use case.

# Airtable tables (identifiers/config).
# Note: keep names aligned with whatever your code expects (some codebases use WAITLIST vs TABLE_NAME).
#AIRTABLE_WAITLIST: This is a conditional env var, not required for every use case.
#AIRTABLE_FEATURES: This is a conditional env var, not required for every use case. 
#AIRTABLE_BUGS: This is a conditional env var, not required for every use case.

# Airtable token (SECRET) — keep placeholder only.
#AIRTABLE_TOKEN: This is a conditional env var, not required for every use case.

# HubSpot access token (SECRET) — keep placeholder only.
# HUBSPOT_ACCESS_TOKEN="HUBSPOT_ACCESS_TOKEN_PLACEHOLDER"

# Email notification recipient (not necessarily secret, but keep as placeholder for templates).
#NOTIFICATION_EMAIL="notifications@example.com"

# Nodemailer credentials (SECRETS) — placeholders only.
#EMAIL_USER="EMAIL_USER_PLACEHOLDER"
#EMAIL_PASS="EMAIL_PASS_PLACEHOLDER"

# Vercel-provided system env var (auto-set on Vercel; usually not set locally). 
# VERCEL_ENV="development"

# Custom Vercel target env label (set on Vercel for staging if you use it).
# NEXT_PUBLIC_VERCEL_TARGET_ENV="staging"

# (NEXT_PUBLIC) Action logger console output level.
# Options: "off", "minimal", "normal", "verbose"
# Defaults to "normal" in development, "minimal" in production.
# NEXT_PUBLIC_ACTION_LOGGER_LOG_LEVEL="minimal"

# (Server-only) Google Gemini API key for AI Trace Analysis (virtual debugger when user is stuck).
# GEMINI_API_KEY="GEMINI_API_KEY_PLACEHOLDER"
